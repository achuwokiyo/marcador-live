## Packages
react-qr-code | Generating QR codes for the share modal
canvas-confetti | Celebration effect when match finishes
@types/canvas-confetti | Types for confetti

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
  sans: ["var(--font-sans)"],
}
